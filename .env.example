# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=taskmanager
DB_SSLMODE=disable

# Application Configuration
STORAGE_TYPE=postgres  # Options: json, postgres
JSON_FILE_PATH=tasks.json

# API Server Configuration
API_ADDR=:8080
